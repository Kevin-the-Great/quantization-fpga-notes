# 每周维护与组会准备

维护范围固定为**量化算法**和 **FPGA 架构**两条主线。每次收录先标明所属主线与具体问题；HLS、映射和工具资料用于支撑 FPGA 架构研究。VLA 等模型类别用于说明应用条件。

## 一周的轻量循环

下面是建议投入量，可按实验和组会安排调整。

| 环节 | 建议投入 | 留下什么 |
|---|---|---|
| 发现 | 约 30 分钟，初筛 5–10 项 | 用会议论文集、期刊新增、预印本、引用和作者线索找到候选；记录真正相关的项目 |
| 判断 | 约 15 分钟 | 为候选写一句收录理由，选出本周的核心问题与主论文 |
| 精读 | 按论文难度分配，通常先完成 1 篇 | 方法、前提、实验配置和证据定位清楚的阅读卡片 |
| 比较 | 约 30 分钟 | 与至少 1 篇已收录资料比较，补一个新结论或修正旧判断 |
| 归档 | 约 10 分钟 | 更新目录状态和周记录，留下下周的一项问题 |

会议新论文集中发布时，安排一次目录扫描；平时分别检查两条主线的增量。精读可以按周交替，也可根据当前问题调整，不要求每周各完成一篇。没有高价值新增时，继续读经典论文、追引用或核对工程实现。

## 收录与更新

1. 先搜索 `data/papers.csv`，同一工作不同版本尽量沿用同一 ID，避免 arXiv 与正式版重复计数。
2. 更新主目录，填写 `tracks` 与技术标签；正式发表信息以会议论文集、出版页或明确的官方接收记录为准。未确定时填 `unconfirmed`。
3. 使用 `templates/paper-note.md` 创建 `notes/<id>.md`。笔记记录自己实际查看的章节，不把读过摘要写成全文精读。
4. 将精读中得到的比较更新到 `comparisons/`；需要数字时，按配置写入 `data/measurements.csv`，保留来源定位。
5. 使用 `templates/weekly-note.md` 记录本周实际完成、改变的认识与下一步。

`papers.csv` 是题名、发表信息和阅读状态的主记录。README 是导航，对比表保存研究判断，阅读笔记保存推理与证据。发表信息变动时，顺带修正笔记或对比表中重复出现的引用。

## 进度由实际工作更新

`reading_status` 可用：

| 值 | 含义 |
|---|---|
| `unrecorded` | 尚未登记个人阅读情况，不表示没有读过 |
| `queued` | 已明确排入待读 |
| `skimmed` | 已完成摘要、主要图表或核心章节初读 |
| `deep_read` | 已核对方法与主要实验，并形成自己的阅读笔记 |
| `reproduced` | 完成一个明确范围的复现；笔记必须写清复现了什么 |
| `deferred` | 暂缓，记录原因即可 |

汇报日期单独写 `presented_on`，不把“讲过”当作“复现过”。复现过一个算子，也不等于复现了论文完整系统。

## 轮到组会时

围绕量化算法或 FPGA 架构中的一个问题，采用“一篇主论文 + 一到两项对照”的组织方式。也可以针对位宽与数据通路等共同问题连接两条主线；每次汇报不必同时覆盖两者。每个部分留下可讨论的结论：

1. 问题为什么值得解决，哪些约束决定了解法。
2. 主论文的核心机制，用一张图或一个小例子解释。
3. 对照方法在哪些条件下采用了不同选择。
4. 实验证据能支持到什么程度。
5. 对当前方向提出一项具体判断，以及一个待验证问题。

使用[汇报提纲模板](templates/talk-outline.md)。先整理论点、比较表和关键原图出处，再制作幻灯片。汇报时长与轮次日期按实际组会安排填写。

## 第一版之后

1. 登记已经读过的种子论文，跳过重复阅读，把时间放在尚未解决的问题上。
2. 完成第一篇阅读卡片及一次横向比较，再据此调整字段，避免维护不常使用的信息。
3. 在 GitHub 仓库 `quantization-fpga-notes` 持续更新；增加资料时同步维护首页索引、主目录和相应比较表。
4. 连续维护两轮后，再决定是否需要自动检索、自动生成索引或可视化页面。
